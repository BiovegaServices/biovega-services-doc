meta {
  name: Get metadata
  type: http
  seq: 3
}

get {
  url: {{server}}/odata/v1/$metadata
  body: none
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

tests {
  test("status is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
