meta {
  name: Get Products expand StoreInventory
  type: http
  seq: 10
}

get {
  url: {{server}}/odata/v1/products?companyId={{companyId}}&$expand=StoreInventory
  body: none
  auth: bearer
}

params:query {
  companyId: {{companyId}}
  $expand: StoreInventory
}

auth:bearer {
  token: {{access_token}}
}

tests {
  test("status is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });

  test("returns a result list", function() {
    const body = res.getBody();
    const rows = body.value ?? body;
    expect(rows).to.be.an("array");
  });
}
