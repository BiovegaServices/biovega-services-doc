meta {
  name: Get Products expand ProductPrice
  type: http
  seq: 8
}

get {
  url: {{server}}/odata/v1/products?companyId={{companyId}}&$expand=ProductPrice
  body: none
  auth: bearer
}

params:query {
  companyId: {{companyId}}
  $expand: ProductPrice
}

auth:bearer {
  token: {{access_token}}
}

tests {
  test("status is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });

  test("returns a result list", function() {
    const body = res.getBody();
    const rows = body.value ?? body;
    expect(rows).to.be.an("array");
  });
}
